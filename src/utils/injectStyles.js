// Import the CSS file directly
import '../styles/algolia-widgets.css';

// Function to inject CSS into head (keeping as a placeholder in case we need additional dynamic styles)
export function injectStyles() {
  console.log('Styles are automatically injected via import');
}
